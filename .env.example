# Copy this file to .env for local `netlify dev` testing (never commit the real .env).
# In production (Netlify), set these under Site settings > Environment variables instead.

ANTHROPIC_API_KEY=sk-ant-...
AI_MODEL=claude-sonnet-5
