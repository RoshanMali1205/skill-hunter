{
  "name": "skill-hunter-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MCP server that exposes Skill Hunter learning topics as tools, resources, and prompts.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "tsx src/server.ts",
    "test": "tsx --test src/*.test.ts",
    "typecheck": "tsc --noEmit",
    "inspect": "npx --yes @modelcontextprotocol/inspector@latest npm start",
    "inspect:search": "npx --yes @modelcontextprotocol/inspector@latest --cli npm start --method tools/call --tool-name search_topics --tool-arg 'query=lazy loading'"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.5.2"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "@types/node": "^26.4.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2"
  }
}
